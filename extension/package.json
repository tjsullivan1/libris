{
  "name": "libris-extension",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "The Libris browser extension. No bundler and no runtime dependencies - it is sideloaded unpacked, and everything here is for tests and lint.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "happy-dom": "^20.14.0",
    "vitest": "^5.0.0"
  }
}
